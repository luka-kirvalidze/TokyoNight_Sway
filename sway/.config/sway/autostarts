# only meant for voidlinux.
# exec pipewire
# exec pipewire-pulse

#output * bg $HOME/dotfiles/wallpapers/a_man_sitting_in_a_chair.png fill
exec swaybg -i ~/dotfiles/wallpapers/matrix_original.png -m fill

# these auto-starts are needed for screen-sharing.
exec --no-startup-id dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP=sway
exec --no-startup-id /usr/lib/xdg-desktop-portal-wlr
exec --no-startup-id /usr/lib/xdg-desktop-portal -r
exec lxpolkit
exec swaync
